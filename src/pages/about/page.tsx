import ScrollToTop from '@/components/ScrollToTop';

const About = () => {
    return (
        <div>
            {/* Existing content */}
            <ScrollToTop />
        </div>
    );
};

export default About;